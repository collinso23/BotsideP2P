"""
File will contain static variables which are needed on all machines 
The file shall ideally be encrypted and dropped onto machines where they can decrypt the information.
Example Vars:
    > Bootstrap server IP
    > Network Key
    > Command list

"""
NETWORK_SECRET="TheSecretKey"
DEFAULT_PORT="8468"
BOOTSTRAP_SERVERS=[""]
